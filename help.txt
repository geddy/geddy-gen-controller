Geddy MVC controller generator.

Generates a geddy controller for you.

Usage:

    geddy gen controller create[name]

        name        Name of your controller
        -f --force  (optional) use to replace an existing controller